{
  "name": "rules_system_deps",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",
  "postCreateCommand": "sudo apt-get update -qq && sudo apt-get install -y --no-install-recommends pkg-config libpci-dev libgtk-4-dev libglib2.0-dev && sudo curl -fsSL -o /usr/local/bin/bazel https://github.com/bazelbuild/bazelisk/releases/latest/download/bazelisk-linux-amd64 && sudo chmod +x /usr/local/bin/bazel",
  "remoteUser": "vscode"
}
